# William Bot Configuration Example for Docker Compose
# Copy this file to .env and fill in your actual values

# Required: Telegram Bot Token from @BotFather
TG_BOT_TOKEN=your_telegram_bot_token_here

# Required: OpenAI API Key
OPENAI_API_KEY=your_openai_api_key_here

# Optional: OpenAI Model (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Optional: Message buffer size before auto-summarization (default: 100)
MAX_MSG_BUFFER=100

# Optional: Max tokens per GPT request (default: 2048)
CTX_MAX_TOKENS=2048

# Optional: Timezone for midnight processing (default: Europe/Belgrade)
TZ=Europe/Belgrade

# Optional: Custom config path (default: config/app.toml)
APP_CONFIG_PATH=config/app.toml

# Database connection (automatically set in docker-compose)
# PG_DSN=postgres://william:william_password@postgres:5432/william?sslmode=disable 